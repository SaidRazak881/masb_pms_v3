import {createClient} from '@/lib/supabase/server'
export default async function Dashboard(){const s=await createClient();const [{count:programs},{count:invoices},{count:actions},{count:sessions}]=await Promise.all([s.from('programs').select('*',{count:'exact',head:true}),s.from('invoices').select('*',{count:'exact',head:true}),s.from('vw_action_required').select('*',{count:'exact',head:true}),s.from('training_sessions').select('*',{count:'exact',head:true})]);return <div className='p-6'><h1 className='text-2xl font-bold'>Dashboard</h1><p className='mt-1 text-sm text-slate-500'>Operational overview daripada Supabase.</p><div className='mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4'>{[['Programs',programs],['Invoices',invoices],['Action Items',actions],['Training Sessions',sessions]].map(([a,b])=><div key={String(a)} className='rounded-xl border bg-white p-5 shadow-sm'><div className='text-sm text-slate-500'>{a}</div><div className='mt-2 text-3xl font-bold'>{b??0}</div></div>)}</div></div>}
