'use client'
import {useState} from 'react'
import {createClient} from '@/lib/supabase/client'
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const {error}=await createClient().auth.signInWithPassword({email,password});if(error)setError(error.message);else location.href='/dashboard';setLoading(false)}return <main className='flex min-h-screen items-center justify-center p-6'><form onSubmit={submit} className='w-full max-w-md rounded-2xl border bg-white p-8 shadow-sm'><h1 className='text-2xl font-bold'>MIMOS Academy</h1><p className='mt-1 mb-6 text-sm text-slate-500'>Log masuk ke sistem pengurusan R1/R2/R3.</p><label className='text-sm font-medium'>Email<input required type='email' value={email} onChange={e=>setEmail(e.target.value)} className='mt-1 mb-4 w-full rounded-lg border p-3'/></label><label className='text-sm font-medium'>Password<input required type='password' value={password} onChange={e=>setPassword(e.target.value)} className='mt-1 mb-4 w-full rounded-lg border p-3'/></label>{error&&<div className='mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700'>{error}</div>}<button disabled={loading} className='w-full rounded-lg bg-slate-900 p-3 font-medium text-white disabled:opacity-50'>{loading?'Signing in…':'Sign in'}</button></form></main>}
