import Link from "next/link";
import { signOut } from "@/app/(auth)/actions";
import { requireUser } from "@/lib/rbac/guards";
const nav=[['/','Dashboard'],['/action-center','Action Center'],['/programs','Program 360'],['/quotations','Quotations'],['/financials/invoices','Financials'],['/training','Training'],['/reports','Reports'],['/data-quality','Data Quality']];
export default async function DashboardLayout({children}:{children:React.ReactNode}) { const {supabase,user}=await requireUser(); const {data:profile}=await supabase.from('profiles').select('full_name,role').eq('id',user.id).single(); return <div className="min-h-screen bg-slate-50"><aside className="fixed inset-y-0 left-0 hidden w-64 border-r bg-white p-5 lg:block"><div className="mb-8"><div className="text-xs font-bold tracking-widest text-sky-700">MIMOS ACADEMY</div><div className="text-lg font-bold">PMS</div></div><nav className="space-y-1">{nav.map(([href,label])=><Link key={href} href={href} className="block rounded-lg px-3 py-2 text-sm font-medium text-slate-600 hover:bg-slate-100 hover:text-slate-900">{label}</Link>)}</nav><form action={signOut} className="absolute bottom-5 left-5 right-5"><button className="w-full rounded-lg border px-3 py-2 text-sm">Sign out</button></form></aside><main className="lg:pl-64"><header className="sticky top-0 z-10 flex h-16 items-center justify-between border-b bg-white/95 px-6 backdrop-blur"><div className="font-semibold">MIMOS Academy PMS</div><div className="text-right"><div className="text-sm font-medium">{profile?.full_name ?? user.email}</div><div className="text-xs uppercase text-slate-500">{profile?.role ?? 'viewer'}</div></div></header><div className="p-6">{children}</div></main></div> }
