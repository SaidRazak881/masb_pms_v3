import { requireUser } from "@/lib/rbac/guards";
export default async function TrainingPage(){const {supabase}=await requireUser();const {data}=await supabase.from('vw_r2_overall_report').select('*').order('start_date',{ascending:false});return <div><div className="mb-6"><p className="text-sm font-semibold text-sky-700">R2 TRAINING</p><h1 className="text-3xl font-bold">Training delivery</h1></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data?.map((s:any)=><div key={`${s.program_code}-${s.session_title}-${s.category}`} className="rounded-xl border bg-white p-5"><div className="text-xs text-slate-500">{s.program_code}</div><h2 className="mt-1 font-bold">{s.session_title}</h2><p className="mt-2 text-sm text-slate-500">{s.company_name} · {s.start_date||'TBC'}</p><div className="mt-4 flex gap-4 text-sm"><span>Bumi: <b>{s.bumiputera_count||0}</b></span><span>Non-Bumi: <b>{s.non_bumiputera_count||0}</b></span></div></div>)}</div></div>}
