import Link from "next/link";
import { requireUser } from "@/lib/rbac/guards";
import { formatMYR } from "@/lib/utils";
export default async function ProgramsPage(){const {supabase}=await requireUser(); const {data}=await supabase.from('programs').select('id,program_code,title,current_stage,forecast_value,weighted_value,sector,companies(canonical_name)').order('updated_at',{ascending:false}); return <div><div className="mb-6 flex items-end justify-between"><div><p className="text-sm font-semibold text-sky-700">PROGRAM 360</p><h1 className="text-3xl font-bold">Programs</h1></div></div><div className="overflow-hidden rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Code</th><th className="p-4">Program</th><th className="p-4">Company</th><th className="p-4">Stage</th><th className="p-4">Forecast</th><th className="p-4">Weighted</th></tr></thead><tbody>{data?.map((p:any)=><tr key={p.id} className="border-t hover:bg-slate-50"><td className="p-4"><Link className="font-semibold text-sky-700" href={`/programs/${p.id}`}>{p.program_code}</Link></td><td className="p-4">{p.title}</td><td className="p-4">{p.companies?.canonical_name}</td><td className="p-4"><span className="rounded-full bg-slate-100 px-2 py-1 text-xs">{p.current_stage}</span></td><td className="p-4">{formatMYR(p.forecast_value)}</td><td className="p-4 font-semibold">{formatMYR(p.weighted_value)}</td></tr>)}</tbody></table></div></div>}
