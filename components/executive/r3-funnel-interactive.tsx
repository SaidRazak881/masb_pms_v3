'use client'
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'
import { Tabs, TabsList, TabsTrigger, useTabsValue } from '@/components/ui/tabs'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
export type R3FunnelData = { stages: { name: string; count: number; value?: number }[]; forecast: number; weighted: number; secured: number; count: number }
export function R3FunnelInteractive({ data }: { data: R3FunnelData }) { return <Card><CardHeader className="flex-row items-center justify-between space-y-0"><div><CardTitle className="text-base">R3 Pipeline & Funnel</CardTitle><p className="mt-1 text-xs text-slate-500">Lead → Proposal → Contract → Secured</p></div><Tabs defaultValue="count"><TabsList><TabsTrigger value="count">Kiraan</TabsTrigger><TabsTrigger value="value">Nilai RM</TabsTrigger></TabsList><ChartMode data={data}/></Tabs></CardHeader><CardContent><div className="grid grid-cols-3 gap-3 mb-5"><div className="rounded-lg bg-slate-50 p-3"><p className="text-xs text-slate-500">Forecast</p><p className="mt-1 font-semibold tabular-nums">RM {data.forecast.toLocaleString('en-MY')}</p></div><div className="rounded-lg bg-indigo-50 p-3"><p className="text-xs text-slate-500">Weighted</p><p className="mt-1 font-semibold tabular-nums">RM {data.weighted.toLocaleString('en-MY')}</p></div><div className="rounded-lg bg-emerald-50 p-3"><p className="text-xs text-slate-500">Secured</p><p className="mt-1 font-semibold tabular-nums">RM {data.secured.toLocaleString('en-MY')}</p></div></div></CardContent></Card> }
function ChartMode({data}:{data:R3FunnelData}) { const mode=useTabsValue(); const rows=data.stages.map(s=>({name:s.name,count:s.count,value:s.value??0})); return <div className="absolute left-6 right-6 top-[190px]"><ResponsiveContainer width="100%" height={280}><BarChart data={rows} margin={{top:10,right:10,left:-20,bottom:0}}><CartesianGrid vertical={false} strokeDasharray="3 3"/><XAxis dataKey="name" tick={{fontSize:11}}/><YAxis allowDecimals={false} tick={{fontSize:11}}/><Tooltip/><Bar dataKey={mode==='value'?'value':'count'} name={mode==='value'?'Pipeline Value':'Programs'} fill={mode==='value'?'#6366f1':'#2563eb'} radius={[5,5,0,0]}/></BarChart></ResponsiveContainer><p className="mt-2 text-xs text-slate-500">Velocity index: {data.forecast>0?((data.secured/data.forecast)*100).toFixed(1):'0.0'}% secured / forecast</p></div> }
