import { requireUser } from "@/lib/rbac/guards";
export default async function QuotationsPage(){const {supabase}=await requireUser();const {data}=await supabase.from('quotations').select('id,quotation_no_raw,quotation_date,final_price,status,prepared_by,programs(program_code,title)').order('quotation_date',{ascending:false});return <div><div className="mb-6"><p className="text-sm font-semibold text-sky-700">COMMERCIAL</p><h1 className="text-3xl font-bold">Quotations</h1></div><div className="overflow-hidden rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="p-4">Quotation</th><th className="p-4">Program</th><th className="p-4">Date</th><th className="p-4">Value</th><th className="p-4">Status</th></tr></thead><tbody>{data?.map((q:any)=><tr key={q.id} className="border-t"><td className="p-4 font-medium">{q.quotation_no_raw}</td><td className="p-4">{q.programs?.program_code} · {q.programs?.title}</td><td className="p-4">{q.quotation_date||'—'}</td><td className="p-4">RM {(q.final_price||0).toLocaleString()}</td><td className="p-4">{q.status}</td></tr>)}</tbody></table></div></div>}
